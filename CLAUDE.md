# 車をだせ！（car-escape）

ラッシュアワー型のスライドパズル。6×6の駐車場で車をスライドさせ、赤い車を右端の出口から出す。
単一HTML（`index.html`）。ブラウザで開くだけで動く。ライブラリ・APIキー不要。

## 正典地図
- 仕様（盤面・操作・レベル・完成条件）: `docs/spec.md`
- AIガイドライン（触るなリスト）: `docs/ai_guidelines.md`
- 決定ログ: `docs/decision_log.md`（迷って決めた判断は Vault `10.Projects/車をだせ/90_開発ログ.md` にも書く）
- レベル生成器: `tools/gen_levels.py`（BFSで最短手数を検証済みのものだけ埋め込む）
- 思想: `../../template/owner_context.md`（コピー禁止）

## 憲法（変えない原則）
- 面白さの核は「盤面を見て、順番を考えて、抜ける」こと。タイマー・スコア・敵・広告的な演出は足さない
- 盤は6×6固定。赤い車は3段目（上から3行目）で右の出口へ。この2つは変えない
- 1手＝1台を好きなマスだけ滑らせること（標準のラッシュアワー流儀）。最短手数はBFSで機械的に出す。手で書かない
- レベルは全部叩き台。最短手数と「実際に解ける」ことを機械で検証してから埋め込む
- 実機（ブラウザ・スマホ）で遊ぶまで「完成」と言わない
- 迷って決めた判断が出たら、決定ログへの記録をユーザーに促すこと

## 作業ルール
- 日本語。編集対象は `index.html`・`docs/`・`tools/`
- ローカルプレビュー: ルート `.claude/launch.json` の "car-escape"（python http.server 8733）
- 実装した本人は検証しない（qa係）。文言・画面は ui係
- 日本酒テーマは**入れない**（2026-09-06決定。入れるなら決定ログを経てから）
