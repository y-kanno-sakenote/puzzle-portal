担当: 🧪 qa係

# くるくる 初版 独立検証（2026-09-13）

対象: `tools/puzzles.json`（40問）＝ `index.html` 埋め込み PUZZLES と完全一致（diff済み）。
独立ソルバーは実装者コードを読まずに新規実装（`tools/qa_check.py`）。探索順は列優先（実装者は行優先）。
n=2,3 総当たり300試行との自己突合わせは mismatch=0。

## 1. 独立ソルバーでの解の数と一致 — PASS
40問すべて解1個。得られた解は全問 `solution` フィールドと完全一致。
実行: `python3 tools/qa_check.py tools/puzzles.json` → `[40問] bad=0件`

## 2. 盤面妥当性 — PASS
- `solution` は全マスの腕が過不足なく接続し単一の木（connected_all=true、腕0マス無し）
- `cells` は `solution` の各マスをそのまま回転したもの（腕の本数・種類が一致）
- `cells` 単体では未解決（connected_all(cells)=false）
- `cells` 文字列の重複なし（40件とも別）

## 3. 出題の質 — PASS
最初から正解のマスの割合、最大は idx=12（初級）の 16/36=44.4%。全問50%未満（憲法「半分以上は捨てる」を満たす）。

## 4. JS実測（Node, index.htmlの先頭scriptをそのままeval） — PASS
- `countSolutions` 40問すべて1、最長2ms
- `isSolved(solution)`=true、`isSolved(cells)`=false（全問）
- `rotate(m,4)===m`（0〜15全パターン）

## 5. localStorage壊れ値（vm+DOMスタブでui側scriptも実走） — PASS
非JSON／null／数値／文字列／配列／`last`小数・負・範囲外／`cleared`型不正／`rot`型不正・値の型不正・長さ不正・不正16進／`getItem`throw／`setItem`throw の15パターン、全て例外なく起動。

## 6. 難度のノード数分布 — PASS（矛盾なし）
自前ソルバー（列優先）でも尻上がりに増加し、spec.mdの主張（盤サイズにほぼ比例）と矛盾なし。
- 入門27〜40 / 初級45〜94 / 中級79〜227 / 上級240〜540（実装者側の行優先nodesは32〜51/57〜96/113〜203/242〜409）
- `gen_puzzles.py --seed 20260913` を再実行し `tools/puzzles.json` とバイト完全一致を確認（再現性あり）

## 7. spec.md と実装の齟齬 — PASS（齟齬なし）
ルール表示2行・操作（タップのみ）・undo/reset/next・localStorageキー構造・光り方（左上起点の接続集合）とも一致。
禁止機能（逆回転／長押し／ドラッグ／手数カウンター／タイマー／ライフ／ヒント／自動解答／効果音）は実装に無し。

## 【要確認】
- JS側 `countSolutions`/`variants` は生成器(Python)とほぼ同一アルゴリズム（同じ枝刈り・union-find構造）の移植であり、真に独立ではない。40問一致は確認したが、アルゴリズム自体に共通のバグがあれば両方すり抜ける。今回の合否は本報告の列優先・別実装ソルバー（項目1・6）を根拠にした
- 実機（スマホでのタップ操作・光り方の見え方）は未検証。担当外

## 未検証
- 実機プレイ（スマホ実機でのタップ判定・アニメーション・光りの見え方）

以上、直すべき点なし。
