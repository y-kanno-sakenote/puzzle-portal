担当: ✅ 検証係（マンガー×ファインマン）

# すべりロボ v0.1 独立検証レポート（2026-09-07）

実装者（Opusサブ）報告「Python/JS 40本一致・自動プレイ40/40・needsOthers 中級上級20本・JS solve最長57ms」を、実装コードを一切呼ばないクリーンルーム実装で再検証した。
独立ソルバー: `tools/qa_solver.py`（BFS再実装。状態=対象色の体の位置＋他3体位置をソートしたタプル）

## 結果一覧

| # | 項目 | 判定 | 根拠 |
|---|---|---|---|
| 1 | 盤面妥当性（144文字・壁の整合・中央塊・的のL字角・4体重複無し） | PASS | 40/40 エラー無し |
| 2 | 独立BFS: 40本の最短手数・tier | PASS | 40/40 一致（qa_min/impl_min, qa_tier/impl_tier とも全一致） |
| 3 | needsOthers（的の色だけで解けるか）自前再計算 | PASS | 40/40 一致（入門初級20本=false、中級上級20本=true） |
| 4 | 盤面重複・embed一致 | PASS | 重複0本／index.html埋め込みとtools/levels.jsonが完全一致 |
| 5 | JS: SlideRobots.solve() 40/40一致 | PASS | 全一致、最長325ms（#37, #39付近。実装者報告の57msより遅いのはマシン差＋Node v26起動込みの計測） |
| 6 | 境界値（slide/applyMove: 不正id・不正方向・壁際0マス） | PASS | bad_id/bad_dir は全てnull、applyMoveは元stateを変えない |
| 6' | 【追加発見】state.robotsを盤外座標に直接書き換えてslide()を呼ぶ | FAIL（未知の危険） | 3秒でタイムアウト＝無限ループ。walls[cur]が配列外(undefined)のとき `undefined & BIT[d]` が常に0になり境界チェックが効かず、cur+=stepを無限に続ける。ただしparse/applyMove/solveの正規経路ではrobotsが盤外に出ることは無いため、UIから到達はしない防御の穴 |
| 7 | 自動プレイ（自前最短経路をapplyMoveで適用→isSolved） | PASS | 40/40 成功 |
| 8 | localStorage壊れ値（非JSON・last小数・負数・巨大値・cleared文字列・null・getItem例外） | PASS | 7ケース全てクラッシュ無し |
| 9 | spec.mdとの齟齬（類似判定の読み替え） | 妥当と判断 | spec.md「壁文字列の一致率≤0.7」を実装は「壁のあるマスだけの一致率」に読み替え。decision_log.mdに理由（単純文字列一致だと外周・中央塊・壁なしマスが常に一致し下限0.83になり閾値0.7では全滅）と実測値（相互最大0.19）が明記済み。判断ログに残っており妥当 |

## 総合判定: PASS（1件の追加発見あり）

40本のBFS一致・JS一致・自動プレイ・localStorage耐性はすべて実測でPASSした。実装者報告の数値（40/40一致、40/40自動プレイ、needsOthers中級上級20本）は誇張なく確認できた。

追加で見つけたのは、`state.robots` を正規APIを経由せず直接盤外座標へ書き換えてから `slide()` を呼ぶと無限ループするという防御の穴（6'）。これは現在のUI（`onDown`→`SlideRobots.slide(state, drag.id, dir)`、`applyMove`が返すstateしか使わない）からは到達しない経路であり、**現時点でユーザーに影響しない**。直すかどうかは実装側の判断に委ねる（優先度は低いと考える＝足さない案：このためだけに境界チェックを足すのは今は不要。次に状態を外部から書き換える機能（デバッグ用シード復元など）を作るときに一緒に直せば足りる）。

## 直すべき順（最大3つ）
1. 【任意・低優先】`slide()` に `cur` の盤内範囲チェック（`cur<0 || cur>=W*W` で打ち切り）を足すか、少なくとも「state.robotsは常にparse/applyMove由来である」という前提をコード注記に残す（6'の危険を将来のUI変更が踏まないため）
2. 特になし（他は全PASS）
3. 特になし

## 検証環境
- Node v26.0.0 / Python 3.14
- 独立BFSの実行時間: 40本合計で数秒（最長は上級#39の300ms級）
- ログ全文: `/private/tmp/claude-501/-Users-ymacmini-Documents-claudecode-macmini/84792f8b-2118-477f-8a4f-455403769681/scratchpad/qa_full_log.txt`（セッション固有の一時領域。恒久保存はしていない）
