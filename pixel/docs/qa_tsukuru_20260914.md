# ピクセル作成モード 検証（2026-09-14・本体が実施）

qa係に独立検証を出したが報告が途中で切れ、成果物も残らなかったため、本体が主要な項目を自分で確かめた。
**qa係による独立検証は未実施**。次のセッションで出し直す価値がある（特に、自前のラインソルバーとの突き合わせ）。

| 項目 | 結果 | 根拠 |
|---|---|---|
| 移植の正しさ（解ける） | PASS | 同梱60問の `solution` → `hints` → `lineSolve` が **60/60** で解け、解答も一致 |
| 移植の正しさ（難度D） | PASS | `rate` の D が `tools/gen_puzzles.py` の値と **60/60** 一致 |
| 判定4通り | PASS | 空盤→`empty`／入れ替え可能な2×2→`multi`／全部塗り→`ok`（入門）／1マス→`ok`（入門） |
| ハッシュ往復 | PASS | 名前に `.`・`#%&?=`・絵文字・空・40字・壊れたサロゲートを含む21通りで往復一致。壊れたハッシュ5種はすべて `null` |
| URLの長さ | PASS | 25×25 で 143文字 |
| 開いた時の再検証 | PASS | 解が2通りの盤のURLで開くと「この問題は開けません／答えが2通り以上あるので、解ける問題になっていません。」で弾く |
| 自動修復が無いこと | PASS | qa係の断片報告で確認（マスを勝手に反転させる処理なし）。足さないもの（ヒント・タイマー・ライフ・色・取り込み）の混入もなし |
| 既存への影響 | PASS | `index.html` の差分は**リンク1行のみ**。`tsukuru.html` は `pixel-v2` を一切読み書きしない（保存キーは `pixel-tsukuru`） |

## 未確認
- qa係の自前ソルバーとの突き合わせ（移植元の生成器と tsukuru は同じ考え方なので、これだけでは独立検証にならない）
- `guess`（伝播だけでは解けないが解は一つ）の判定を狙って作った例での確認
- localStorage の壊れ値での起動
- 実機（スマホ）で描く手触り。作る画面に拡大が無く、25×25 は1マス13px
