担当: ✅ 検証係（マンガー×ファインマン）

# 箱入り娘 v0.1 独立検証レポート（2026-09-07）

対象: `index.html`（純粋ロジック `Hakoiri` + 埋め込み `LEVELS` 40本）・`tools/levels.json`・`tools/gen_levels.py`
独立ソルバー: `tools/qa_solver.py`（クリーンルーム実装。実装者コードは呼ばない）
実行: `python3 tools/qa_solver.py`（Node呼び出しは一時JSをスクラッチに書いてsubprocess実行、リポジトリは汚さない）

## 判定: PASS

| # | 検証項目 | 判定 | 根拠 |
|---|---|---|---|
| 1 | 自前BFSで40本の最短手数・tier一致 | PASS | 40/40一致。最長は上級114手盤（678ms）、中級20手盤が最遅3.7s（自前実装は非最適化ゆえ） |
| 2 | 盤面妥当性（20文字／娘1つ2×2／長方形／穴なし／空き≥2） | PASS | エラー0件。空きマス最小2（規定「空きは2マス以上」を満たす下限ちょうど） |
| 3 | 相互Jaccard（(x,y,w,h)集合） | PASS | 最大0.4545（#27×#37、共通5/和11）＝報告値0.45と一致。0.5超ペア0件 |
| 4 | index.html埋め込みLEVELSとtools/levels.jsonの一致 | PASS | 完全一致（JSON比較） |
| 5 | Node: `Hakoiri.solve()` 40/40 | PASS | 全一致。最長616ms（盤 `BAACBAADoEoDFFGHIoJo`、40手） |
| 6 | `canMove`/`applyMove` 境界値 | PASS | 盤外(左/下)・dx=dy=0・斜め・id=-1・id=999は全てfalse／applyMoveはnull返却。衝突で止まる駒・自由に動ける駒の両方を実盤面から確認 |
| 7 | 自動プレイ（自前最短経路→JS `applyMove`→`isSolved`） | PASS | 40/40成功。各手で`canMove`確認後に適用、経路の同一サイズ駒マッチングで実施 |
| 8 | localStorage壊れ値でのクラッシュ有無 | PASS | 非JSON文字列／`last`小数・負数・巨大値／`cleared`が文字列／`null`／`getItem`が例外throw の7ケース全てクラッシュなし（DOMスタブ経由でスクリプト全体を実走） |
| 9 | spec.mdとの齟齬 | 齟齬なし | 手数定義（1手＝1駒を一直線に1マス以上、角折れ2手）・出口座標(1,3)・盤面記法・保存キー全て一致 |

## 実装者報告との照合
「Python/JS 40本一致」「自動プレイ40/40」「横刀立馬は実測90手」「相互Jaccard最大0.45」— 全て独立測定で再現・一致した。虚偽・誇張なし。

## 【要確認】既知の未検証範囲（担当外）
- ブラウザ実機（ドラッグ操作・スナップ・出口演出）は担当外。指示書通り未実施
- 自前BFSは実装のBFSと同じ「同サイズ駒は区別しない」正規化を採用（両者が同じ設計思想で解いているため、双方が同じバグを共有すれば一致してしまう理論上のリスクはゼロではない）。ただしJS版solveも別実装（別コード）で同値なので、3実装（Python独立版・JS版・実装者Python版）が揃って一致しており、リスクは小さいと判断

## 直すべき順（今回は無し）
今回の検証範囲でFAILは0件。差し戻し不要。

## 足さない案
- localStorageの追加ケース（配列・巨大文字列・Unicode異常値）は今回の7ケースで十分。境界拡張は不要と判断
