担当: ✅ 検証係（マンガー×ファインマン）

# はやつかみ 再検証レポート v2（2026-09-13）

対象: ルール裁定変更後（`docs/decision_log.md` 2026-09-13 最上段）の実装。
「先に触れた方」→「先に引き切った方（しきい値到達順）」への変更を受け、`tools/qa_check.py` の先着判定試験を全面的に書き直した。
実行: `python3 tools/qa_check.py`（安定性確認のため5回連続実行、5回とも exit=0 / 総合PASS で一致）。

## 前提（合否基準）
- `docs/spec.md` 22-29行目・`docs/decision_log.md` 最上段を正とする。曖昧点なし。
- 実装(`index.html` の `Grab`)は pending キュー / requestAnimationFrame 待ちを廃し、しきい値到達の瞬間に即時判定する方式に変わっている（280-337, 566-572行目）。よって「先着」はJSイベントループ上の**呼び出し順そのもの**に帰着する。

## 書き直した試験
`tools/qa_check.py` の `run_race_condition_test`（rAFキュー化・pending最小t0方式を前提にした旧テスト）を`run_grab_rules_test` に全面置換。5シナリオを**それぞれ独立したVMコンテキスト**で実行（前シナリオの得点・pointer状態を持ち越すと7枚決着やdone/deadの混線で判定がぶれるため）。「札が出る前」の状態を作るため、サンドボックスの `setTimeout` を即時実行からキュー化＋手動flushに変更。judge発生の有無は note要素のtextContent（'とった！'/'おてつき'/HINT不変）で観測。

## 結果（全項目 PASS、根拠は実測ログ）
1. **先着=しきい値到達順、pointerdown時刻は見ない** — PASS。TOP先touch→引き切り遅延、BOTTOM後touch→先に引き切り。`afterBottom={top:0,bottom:1}`、遅れたTOPの引きは`afterTop`でスコア不変（誤爆なし）。
2. **先に触れたが引き切りが遅い方は取れない** — 上記1と同一実測でPASS（TOPが負ける）。
3. **札が出る前は超えられない／出た後は同じ指で有効** — PASS。deal()前のmoveはnote不変・score 0-0のまま。flushTimers後、同じpointerIdでの再moveでnoteが変化（判定発生）。
4. **前の札の指を離さず次の札で引いても誤爆しない／離して触り直せば取れる** — PASS。持ち越した指(id=9)は次札でnote不変・スコア不変。up→再downで同じidでも有効化を確認。
5. **ほぼ同時の引き切りで二重取得しない** — PASS（top先/bottom先の両方向で実施）。合計スコアは常にちょうど1、得点者は常に先に呼ばれた側。

## 前回PASS項目の再確認
札120枚正解集合・cards.json/embedded CARDS整合・`answerOf`/`isValidCard`・壊れ値10+4パターン・お手つき/7枚決着/localStorage — 全て再実行しPASS（前回と数値一致、実装差分なし）。

## spec / decision_log の反映確認
- `spec.md` 22-29行目「先に引き切った方」「しきい値」「起点リセット」「離すまで戻らない」に書き換え済み。
- `decision_log.md` 最上段（2026-09-13）に「先に触れた方が勝ち」を捨てた案として明記。裁定の記録あり。

## 未検証（担当外）
実機（二人で同時に触る）でのフレーム間隔・タッチ座標の実挙動は担当外。本レポートはVM上の論理再現に限る。

## 直すべき順（最大3つ）
1. なし（今回の検証範囲でFAILなし）。
2. （足さない案）本レポートのシナリオをこれ以上増やさない。実機プレイで体感がズレた場合のみ、その事象を再現するテストを追加する。
3. 参考: シナリオ4の「離さず持ち越し」判定は note の不変性で見ている。将来 note の文言仕様が変わった場合はこの検証点も追随が必要。
