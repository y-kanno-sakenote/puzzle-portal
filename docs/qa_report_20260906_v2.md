担当: ✅ 検証係（マンガー×ファインマン）
# 車をだせ！ 差し戻し再検証（2026-09-07）

対象: `index.html`（sha256 376e4744…d410）／`tools/levels.json`（sha256 a09a7b71…ef2fb1、40本）。前回差し戻し（項目5=上級焼き直し）への対応を検証。編集はこのファイルと `tools/qa_solver.py` のみ。

## 結果
| # | 項目 | 判定 | 根拠 |
|---|---|---|---|
| 1 | 自前BFS再照合 | PASS | `python3 tools/qa_solver.py tools/levels.json` → 40/40 min・tier一致、dup=0、上級 min 19〜31（実装者報告と一致） |
| 2 | 埋め込みLEVELS一致 | PASS | index.htmlのLEVELS-BEGIN/ENDをJSONとしてparseしlevels.jsonと`==`比較→差分0。Node vmでscript実行しCarEscape.solve() 40/40がlevels.jsonのminと一致 |
| 3 | 車形Jaccard | PASS | 40C2=780組で自前算出。最大0.467（#32↔#38 上級同士、#29↔#31 中級↔上級、#21↔#26 中級同士）。0.5超は0組（実装者報告どおり）。上級10本（#30〜#39）の盤面を並べて目視→車の位置・台数（10〜11台）・minが揃って散っており前回のような1〜3台違い家族は見えない |
| 4 | 自動プレイ | PASS | qa_solverの最短経路をNode上のCarEscape.applyMoveへ順適用→isSolved 40/40 |
| 5 | loadSave last=2.5 | PASS | `Number.isInteger(o.last)`が実装済み（index.html:379）。疑似localStorage+最小DOMスタブでlast=2.5を注入→例外なし、フォールバックでLEVELS[0]（elTitle="入門 1"）を表示。last=3（整数）は正しくLEVELS[3]（"入門 4"）を表示し分岐を確認 |

## 注記
- boundary: `canMove(state, undefined, 1)` / `canMove(state, NaN, 1)` は依然TypeError（UIの`dataset.id`はparseIntを通るため到達しない。前回報告と同一・未変更）
- 実機（iPhone Safari）は未検証・担当外

## 再現手順
```
cd dev/car-escape
python3 tools/qa_solver.py tools/levels.json --paths /tmp/paths.json   # 項目1
# 項目2-5: index.htmlの<script>を切り出しNode vmで実行（DOMなし=純ロジック／最小DOMスタブ=loadSave）
```
