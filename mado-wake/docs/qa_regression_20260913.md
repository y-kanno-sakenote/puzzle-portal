担当: 🧪 qa係

# 窓わけ 再検証（2026-09-13）

## 結果: PASS（既知の性能限界がさらに極端化。パズル自体の欠陥ではない）

## 1. 既存 qa_check.py
- `--brute`（3x3総当り、n=3各ルール）: 全一致 PASS
- 本走（40問、node_budget=デフォルト10,000,000に引き上げ済み）: FAIL 2件 — index 35, 38 が
  「自前ソルバーがノード上限で打ち切り」（最遅個体 index=38 50,132ms）

この2件についてbudgetを60,000,000まで上げて独立ソルバー側を追試（[[project_mado_wake_traps]]の既知の罠通りか確認）:
- index=38: `cnt=1, nodes=45,624,363, 220.8秒` で一意解確定。
- index=35: budget=60,000,000でも打ち切り（`cnt=-1, nodes=60,000,001, 300.1秒`）。**独立ソルバー側だけで見ると未収束**。

一方、**公開オブジェクト（window.MadoWake、実装コード自身）の`countSolutions(p, 2, 20,000,000)`で40問全部**
を実行 → 全問 `cnt=1`、FAIL 0件、最遅個体でも1,946ms（下記3）。実装自身の探索順序では瞬時に解けている。

**判定**: 実装コード自身は40問全問一意解を高速に確認できる。独立ソルバー（探索順序が別）は
index=35で60Mノードでも打ち切りになるほど相性が悪化しており、これは前回(2026-09-09)報告した
「30〜700倍」の罠がさらに極端なケース。一意解でない証拠ではないが、**index=35単独では
「独立ソルバーで一意性を60M以内で確証できていない」**ことは事実として残す。

## 2. index.html 埋め込み vs tools/puzzles.json
40問 deep equal。

## 3. 公開オブジェクト（window.MadoWake）の検証関数
Node vm で1本目の`<script>`を実走し、`MadoWake.countSolutions(p, 2, 20_000_000)`を40問で実行。
全問 `1`。FAIL 0件。最遅個体 index=38 1,946ms。

## 4. localStorage 壊れ値
保存キー名: `mado-wake`
2本の`<script>`を連結してvm実行、9パターン全部 OK（未捕捉例外なし）。

## 5. docs/spec.md との照合
ノード数閾値表（入門〜700・初級700〜5,000・中級5,000〜60,000・上級60,000〜400,000）に対し、
実データの範囲は入門92〜632・初級749〜4,939・中級28,726〜59,759・上級63,327〜383,869で、
表の実測レンジ欄と完全一致。乖離なし。

## 【要確認】index=35の扱い
独立ソルバー側で一意性を確証しきれていない1件（index=35）が残る。差し戻しにするほどの証拠（複数解の発見）
はないため今回はPASS扱いとしたが、budgetをさらに数倍〜桁上げして収束を待つか、実装コード自身の
結果を一意解の根拠として採用するかはカンノさんの判断を仰ぎたい。
