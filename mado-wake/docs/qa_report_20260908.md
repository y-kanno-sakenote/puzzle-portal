担当: 🧪 qa係

# 窓わけ 独立検証レポート（2026-09-08）

検証ツール: `tools/qa_check.py`（Python・実装者コード不使用の自前ソルバー）／
`/private/tmp/.../scratchpad/{extract_logic.js, js_check.js, dom_stub.js, storage_check.js}`（Node、一時ファイルにつき本レポートへ結果を転記）

## 1. 一意性（自前ソルバー・Python、40本）
**PASS**（全40本、解1個・自前ソルバーの解が `solution` と同一分割）

- まず正しさの土台を独立に確認: restricted growth string 総当り（領域分割探索とは無関係の別手法）で 3×3・ルール無しの連結2マス以上分割 = **147**（decision_log.md の実測値と一致）。star/size/color/rect/nosame 各ルール単体でも総当りと自前ソルバーが全一致（PASS、`--brute`）
- 40本中35本は候補昇順探索・node_budget=3,000,000で即PASS
- 残り5本（idx 25, 33, 35, 36, 38。いずれも `rect` を含む・7×7〜8×8）は自前ソルバーの探索順（マス番号昇順、元実装はBFS発見順）と rect ルールの相性が悪く、ノード数が元実装の**30〜700倍**に膨らんだ（例: idx35 は公式308,160ノードに対し自前137,571,098ノード・661.6秒）。budgetを引き上げて個別に再実行し、**5本とも解1個・`solution`と同一分割を確認**（idx25 13.3s／idx33 23.7s／idx35 661.6s／idx36 27.2s／idx38 214.3s）。qa_check.py 既定の3,000,000ノードでは打ち切りになる点は既知の制約としてツール側に残す

## 2. solution の妥当性（自前ルール判定）
**PASS** 40本全て、自前実装の `check_rules`（全マス割当・連結・2マス以上・ルール別判定）で違反なし

## 3. 記号の最小性（品質メモ・入門/初級20本、FALSE扱いにしない）
**品質メモ**: 20本中3本で、削っても一意性が保てる記号が残っていた
- idx6（入門）: 8個中2個（マス位置2,18）
- idx9（入門）: 13個中1個（マス位置16）
- idx19（初級）: 26個中1個（マス位置17）
- 原因: `gen_puzzles.py minimize()` は記号をランダム順に1つずつ試す貪欲法（順序依存）。除去順によっては本来削れる記号が残る。バグではなく最小化アルゴリズムの既知の限界

## 4. JS（実際にブラウザで動くコード自体）
**PASS**
- `countSolutions(p,2)` 40本すべて1、最遅 idx38 1911ms（2,000,000ノード上限内）
- `checkRules(p, solution)` 40本すべて `[]`
- `samePartition` 境界: idリラベル→true／1マス違い→false／長さ違い→false／null→false／循環リラベル→true、全PASS

## 5. localStorage 壊れ値（vm + 最小DOMスタブ、jsdom無し）
**PASS** 15パターン（非JSON、null、数値、配列、last=2.5/文字列/負数/巨大、grid長さ不正、grid未知文字、grid非オブジェクト、cleared配列、getItem/setItemが例外を投げる）すべて例外なく起動・`入門1`表示まで到達

## 6. spec.md と実装の齟齬
**PASS（齟齬なし）**
- tools/puzzles.json と index.html 埋め込みPUZZLESは完全一致（40本、diffゼロ）
- ルール組み合わせ内訳（star+size+color 12／star+size+rect 10／size+color 7／size+color+rect 6／size+rect 3／star+size 2＝40）実測と spec.md 記載が一致
- 段階別ノード数実測範囲（入門92〜632／初級749〜4939／中級28726〜59759／上級63327〜383869）spec.md 表と一致
- 各段階ちょうど10本、nosameは0本 → spec.md の記載通り
- 「できた」を押すまで自動判定しない構造を確認（render/endDragに勝敗判定なし、elDoneのclickのみ）

## 直すべき順（最大3つ）
1. qa_check.py 既定ノード予算（3,000,000）だと rect 系8×8の一部で自前検証が打ち切りになる → 検証ツール側の課題（プロダクトコードの欠陥ではない）
2. `minimize()` の貪欲順序依存で削り残しが出る（品質メモ。3/20本、体感に影響する規模ではない）
3. なし（1意性・ルール整合・保存の堅牢性は全てPASS）

## 未検証（担当外）
実機（スマホ）での指なぞり操作・片手プレイ感は担当外。実機確認が必要。
