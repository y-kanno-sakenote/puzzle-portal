担当: qa係

# 数独 初版 独立検証（2026-09-07）

対象: `tools/puzzles.json`（40本）／`index.html` 埋め込み `PUZZLES`／画面コード。
検証コード: `tools/qa_check.py`（Python・実装者コード不使用）／`/private/tmp/.../scratchpad/dom_stub.js`（Node、vmサンドボックス、非成果物）。

## 1. 自前一意性検証
- 自前バックトラック（固定順DFS、gen_puzzles.py とは探索順が別）で40本すべて解1つ・`solution`一致・ヒント一致・`solution`完成盤（行列ブロック1〜9）を確認
- PASS（`python3 tools/qa_check.py` 実行結果: uniqueness_fail/solution_mismatch/hint_mismatch/solution_not_complete すべて0件）

## 2. 自前難度判定
- 自前ソルバー（1手ずつ適用・都度盤面を走査し直す。実装者版は1走査でまとめて適用）で技法優先順は spec と同一（裸シングル→隠れシングル→ロックド候補→裸ペア→隠れペア）
- 40本すべて `tier` と一致（tier_mismatch 0件）。バッチ適用と逐次適用で差が出るケースは無かった

## 3. 盤面妥当性
- 81文字・重複ヒント無し・段階内ヒント数降順・段階ごとの下限（入門36/初級28/中級24/上級17）すべて満たす（length_bad/givens_mismatch/floor_violation/order_violation 0件）

## 4. JS実測（Node、`index.html` 先頭script抜き出し）
- `countSolutions/rate/tierOf` 40本すべて Python（`tools/puzzles.json`）と一致
- 境界値: `isValidPlacement` は n=0/10/-1→false、'5'(文字列)→true（数値化される）。**自分自身のマスは PEERS に含まれないため、ヒントマスに同じ数字を置いても true を返す**仕様（ただし画面コードからは未使用・グレップで確認済み。判定は `judge()`＝埋まった瞬間の `isComplete` のみで行っており、この関数はデッドコード。バグではないが「実質使われていない」ことは記録として残す）
- `countSolutions` の `limit` は空盤で limit=2→2、limit=5→5 と正しく打ち切り。ヒント矛盾盤（同一行に重複）は0を返す

## 5. localStorage 壊れ値
- 非JSON／トップレベルが配列・文字列・null／`last` が小数・負数・範囲外／`grid` が81文字でない・型不正／`notes` の要素数不足・範囲外混入／`getItem`/`setItem` が throw する14パターン全て、画面スクリプト（vmサンドボックスで実走）が例外を投げず初期化まで完了（PASS 14/14）

## 6. 「正誤は埋まってから」の作法（コード読解）
- `judge()` は先頭で未入力マスがあれば即 return（681-684行）→ `inputDigit` は毎回呼ばれるが未完成時は無言。埋まった時だけ `isComplete` 判定し、正解なら `clear` 表示、不正解なら「どこか違う」のみ（場所は言わない）。spec/CLAUDE.md の記述通り

## 総合
全項目 PASS。FAILなし。

## 直すべき順（最大3つ）
1. なし（差し戻し対象の不具合は見つからず）
2. 【任意・軽微】`isValidPlacement` は画面コードから未参照のデッドコード。将来使うなら「自分自身のマス」を弾く分岐が要る点に注意（現状は用途がないので放置でよい）
3. なし

## 未検証（担当外）
- 実機（スマホ）でのタップ操作・見た目・レイアウト崩れは担当外。実機確認が必要
